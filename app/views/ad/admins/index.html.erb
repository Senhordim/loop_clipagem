<div class="row">
  <div class="col-lg-12">
      <h1 class="page-header">Administradores</h1>
  </div>
  <!-- /.col-lg-12 -->
</div>
<!-- /.row -->

<div class="panel panel-default">
  <div class="panel-heading">
      Lista de administradores
  </div>
  <!-- /.panel-heading -->
  <div class="panel-body">

    <div class="row">
      <div class="col-md-12">
        <%= link_to new_ad_admin_path, class: 'btn btn-primary pull-right' do %>
          <i class="fa fa-plus"></i> Criar novo administrador
        <% end %>
        <br>
      </div>
    </div>

    <div class="table-responsive" style="margin-top:10px;">
      <table class="table table-striped table-bordered table-hover">
        <thead>
          <tr>
            <th>#</th>
            <th>Nome</th>
            <th>Email</th>
            <th>Criado em</th>
            <th>Último login em</th>
            <th>Tipo de acesso</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>

          <% @admins.each do |admin| %>
            <tr>
              <td><%= admin.id %></td>
              <td><%= admin.name  %></td>
              <td><%= admin.email  %></td>
              <td><%= admin.created_at.strftime("%d/%m/%Y as %I:%M%p")  %></td>
              <td>
                <% if admin.last_sign_in_at.present? %>
                  <%= admin.last_sign_in_at.strftime("%d/%m/%Y as %I:%M%p")  %>
                <% else %>
                  Nunca efetuou login
                <% end %>
              </td>
<<<<<<< HEAD
              <td><%= admin.role_i18n  %></td>
=======
              <td><%= admin.role_br  %></td>
>>>>>>> df5741926067601d9d61d28aadcea8e8b7c37b02
              <td>
                <%= link_to edit_ad_admin_path(admin), class: 'btn btn-default' do %>
                  <i class="fa fa-edit"></i>
                <% end %>
                <% if current_admin.id != admin.id %>
                  <%= link_to ad_admin_path(admin), method: :delete, class: 'btn btn-danger', data: {confirm: "Você realmente deseja excluir #{admin.email}?"} do %>
                    <i class="fa fa-trash"></i>
                  <% end %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
